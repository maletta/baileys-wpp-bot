---
description: Executa testes com cobertura
agent: build
---
Run the test suite in the workspace(s) affected by current changes.
Use `npm test` in the relevant workspace (apps/backend or apps/frontend).
If tests fail, show the failures with full output and suggest fixes.
Prefer running with `--coverage` flag when available.
